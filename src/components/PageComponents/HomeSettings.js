import React from 'react';
import { Typography } from '@mui/material';

function HomeSettings() {
    return(
    <Typography variant="h6" align="center">Settings render here.</Typography>
    );
}

export default HomeSettings;