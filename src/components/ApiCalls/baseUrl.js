// BaseUrl.js
// This file contains the base URL for the API calls.

// Base URL for the API calls is defined here. A trailing slash is not used here.
const BASE_URL = 'http://localhost:8080' + '/api';

export default BASE_URL;
